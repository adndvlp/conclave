import { Config } from "effect"

function truthy(key: string) {
  const value = process.env[key]?.toLowerCase()
  return value === "true" || value === "1"
}

function falsy(key: string) {
  const value = process.env[key]?.toLowerCase()
  return value === "false" || value === "0"
}

function number(key: string) {
  const value = process.env[key]
  if (!value) return undefined
  const parsed = Number(value)
  return Number.isInteger(parsed) && parsed > 0 ? parsed : undefined
}

const OPENCODE_EXPERIMENTAL = truthy("CONCLAVE_EXPERIMENTAL")
const OPENCODE_DISABLE_CLAUDE_CODE = truthy("CONCLAVE_DISABLE_CLAUDE_CODE")
const OPENCODE_DISABLE_CLAUDE_CODE_SKILLS =
  OPENCODE_DISABLE_CLAUDE_CODE || truthy("CONCLAVE_DISABLE_CLAUDE_CODE_SKILLS")
const copy = process.env["CONCLAVE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT"]

export const Flag = {
  OTEL_EXPORTER_OTLP_ENDPOINT: process.env["OTEL_EXPORTER_OTLP_ENDPOINT"],
  OTEL_EXPORTER_OTLP_HEADERS: process.env["OTEL_EXPORTER_OTLP_HEADERS"],

  OPENCODE_AUTO_SHARE: truthy("CONCLAVE_AUTO_SHARE"),
  OPENCODE_AUTO_HEAP_SNAPSHOT: truthy("CONCLAVE_AUTO_HEAP_SNAPSHOT"),
  OPENCODE_GIT_BASH_PATH: process.env["CONCLAVE_GIT_BASH_PATH"],
  OPENCODE_CONFIG: process.env["CONCLAVE_CONFIG"],
  OPENCODE_CONFIG_CONTENT: process.env["CONCLAVE_CONFIG_CONTENT"],
  OPENCODE_DISABLE_AUTOUPDATE: truthy("CONCLAVE_DISABLE_AUTOUPDATE"),
  OPENCODE_ALWAYS_NOTIFY_UPDATE: truthy("CONCLAVE_ALWAYS_NOTIFY_UPDATE"),
  OPENCODE_DISABLE_PRUNE: truthy("CONCLAVE_DISABLE_PRUNE"),
  OPENCODE_DISABLE_TERMINAL_TITLE: truthy("CONCLAVE_DISABLE_TERMINAL_TITLE"),
  OPENCODE_SHOW_TTFD: truthy("CONCLAVE_SHOW_TTFD"),
  OPENCODE_PERMISSION: process.env["CONCLAVE_PERMISSION"],
  OPENCODE_DISABLE_DEFAULT_PLUGINS: truthy("CONCLAVE_DISABLE_DEFAULT_PLUGINS"),
  OPENCODE_DISABLE_LSP_DOWNLOAD: truthy("CONCLAVE_DISABLE_LSP_DOWNLOAD"),
  OPENCODE_ENABLE_EXPERIMENTAL_MODELS: truthy("CONCLAVE_ENABLE_EXPERIMENTAL_MODELS"),
  OPENCODE_DISABLE_AUTOCOMPACT: truthy("CONCLAVE_DISABLE_AUTOCOMPACT"),
  OPENCODE_DISABLE_MODELS_FETCH: truthy("CONCLAVE_DISABLE_MODELS_FETCH"),
  OPENCODE_DISABLE_MOUSE: truthy("CONCLAVE_DISABLE_MOUSE"),
  OPENCODE_DISABLE_CLAUDE_CODE,
  OPENCODE_DISABLE_CLAUDE_CODE_PROMPT: OPENCODE_DISABLE_CLAUDE_CODE || truthy("CONCLAVE_DISABLE_CLAUDE_CODE_PROMPT"),
  OPENCODE_DISABLE_CLAUDE_CODE_SKILLS,
  OPENCODE_DISABLE_EXTERNAL_SKILLS: truthy("CONCLAVE_DISABLE_EXTERNAL_SKILLS"),
  OPENCODE_FAKE_VCS: process.env["CONCLAVE_FAKE_VCS"],
  OPENCODE_SERVER_PASSWORD: process.env["CONCLAVE_SERVER_PASSWORD"],
  OPENCODE_SERVER_USERNAME: process.env["CONCLAVE_SERVER_USERNAME"],
  OPENCODE_ENABLE_QUESTION_TOOL: truthy("CONCLAVE_ENABLE_QUESTION_TOOL"),

  // Experimental
  OPENCODE_EXPERIMENTAL,
  OPENCODE_EXPERIMENTAL_FILEWATCHER: Config.boolean("CONCLAVE_EXPERIMENTAL_FILEWATCHER").pipe(
    Config.withDefault(false),
  ),
  OPENCODE_EXPERIMENTAL_DISABLE_FILEWATCHER: Config.boolean("CONCLAVE_EXPERIMENTAL_DISABLE_FILEWATCHER").pipe(
    Config.withDefault(false),
  ),
  OPENCODE_EXPERIMENTAL_ICON_DISCOVERY: OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_ICON_DISCOVERY"),
  OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT:
    copy === undefined ? process.platform === "win32" : truthy("CONCLAVE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT"),
  OPENCODE_ENABLE_EXA: truthy("CONCLAVE_ENABLE_EXA") || OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_EXA"),
  OPENCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS: number("CONCLAVE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS"),
  OPENCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX: number("CONCLAVE_EXPERIMENTAL_OUTPUT_TOKEN_MAX"),
  OPENCODE_EXPERIMENTAL_OXFMT: OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_OXFMT"),
  OPENCODE_EXPERIMENTAL_LSP_TY: truthy("CONCLAVE_EXPERIMENTAL_LSP_TY"),
  OPENCODE_EXPERIMENTAL_LSP_TOOL: OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_LSP_TOOL"),
  OPENCODE_EXPERIMENTAL_PLAN_MODE: OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_PLAN_MODE"),
  OPENCODE_EXPERIMENTAL_MARKDOWN: !falsy("CONCLAVE_EXPERIMENTAL_MARKDOWN"),
  OPENCODE_MODELS_URL: process.env["CONCLAVE_MODELS_URL"],
  OPENCODE_MODELS_PATH: process.env["CONCLAVE_MODELS_PATH"],
  OPENCODE_DISABLE_EMBEDDED_WEB_UI: truthy("CONCLAVE_DISABLE_EMBEDDED_WEB_UI"),
  OPENCODE_DB: process.env["CONCLAVE_DB"],
  OPENCODE_DISABLE_CHANNEL_DB: truthy("CONCLAVE_DISABLE_CHANNEL_DB"),
  OPENCODE_SKIP_MIGRATIONS: truthy("CONCLAVE_SKIP_MIGRATIONS"),
  OPENCODE_STRICT_CONFIG_DEPS: truthy("CONCLAVE_STRICT_CONFIG_DEPS"),

  OPENCODE_WORKSPACE_ID: process.env["CONCLAVE_WORKSPACE_ID"],
  OPENCODE_EXPERIMENTAL_HTTPAPI: truthy("CONCLAVE_EXPERIMENTAL_HTTPAPI"),
  OPENCODE_EXPERIMENTAL_WORKSPACES: OPENCODE_EXPERIMENTAL || truthy("CONCLAVE_EXPERIMENTAL_WORKSPACES"),

  // Evaluated at access time (not module load) because tests, the CLI, and
  // external tooling set these env vars at runtime.
  get OPENCODE_DISABLE_PROJECT_CONFIG() {
    return truthy("CONCLAVE_DISABLE_PROJECT_CONFIG")
  },
  get OPENCODE_TUI_CONFIG() {
    return process.env["CONCLAVE_TUI_CONFIG"]
  },
  get OPENCODE_CONFIG_DIR() {
    return process.env["CONCLAVE_CONFIG_DIR"]
  },
  get OPENCODE_PURE() {
    return truthy("CONCLAVE_PURE")
  },
  get OPENCODE_PLUGIN_META_FILE() {
    return process.env["CONCLAVE_PLUGIN_META_FILE"]
  },
  get OPENCODE_CLIENT() {
    return process.env["CONCLAVE_CLIENT"] ?? "cli"
  },
}
